{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/judegordon/hgps/main/schemas/config/components/data.json",
  "type": "object",
  "description": "Configuration for the simulation input data source.",
  "properties": {
    "source": {
      "type": "string",
      "description": "Location of an external packaged dataset used by the simulation."
    },
    "checksum": {
      "type": "string",
      "description": "Checksum used to verify external packaged data integrity."
    },
    "index": {
      "type": "string",
      "description": "Path to a local data index JSON file."
    }
  },
  "oneOf": [
    {
      "required": ["source", "checksum"]
    },
    {
      "required": ["index"]
    }
  ],
  "additionalProperties": false
}